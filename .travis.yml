sudo: required
language: c
c:
  - "latest"

before_install:
  - sudo apt-get install -q youtube-dl ca-certificates

script:
  - wget -q https://github.com/fabianonline/telegram.sh/raw/master/telegram && chmod +x telegram
  - chmod +x *.sh
  - ./script.sh 

notifications:
  email:
    on_success: never
    on_failure: always
